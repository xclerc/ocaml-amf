; -*- mode: lisp -*-

(jbuild_version 1)

(executable
 ((name      test)
  (libraries (AMF core oUnit oUnit.advanced oUnit.threads sexplib))))

(alias
 ((name runtest)
  (deps (test.exe))
  (action (run ${<}))))
